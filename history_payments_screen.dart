import 'package:flutter/material.dart';

class HistoryPaymentsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Historial de Pagos')),
      body: Center(
        child: Text('Pantalla de pagos históricos'),
      ),
    );
  }
}